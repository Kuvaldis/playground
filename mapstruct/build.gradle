plugins {
    id 'net.ltgt.apt' version '0.8'
}

dependencies {
    compile group: 'org.mapstruct', name: 'mapstruct', version: '1.1.0.CR1'
    apt group: 'org.mapstruct', name: 'mapstruct-processor', version: '1.1.0.CR1'
}
